[color]
    ui = auto
[user]
    name = Tyler Santerre
    email = tylers@activestate.com
[core]
    editor = vim
[pull]
	rebase = true
[push]
	default = current
[status]
	submodulesummary = true
[alias]
	pup = "!f(){ git pull \"$@\" && git submodule init && git submodule sync && git submodule update --recursive; }; f"
	pulls = "!f(){ git pull \"$@\" && git submodule update --init --recursive; }; f"
	clones = clone --recursive
[filter "lfs"]
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
[submit-work "pivotaltracker"]
	token = 4f5102af2ce47637eef294ea07d2b7a4
	username = tylersanterre
